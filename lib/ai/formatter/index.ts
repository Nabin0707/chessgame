/**
 * Formatter — lib/ai/formatter/index.ts
 *
 * Output formatting system that transforms raw Gemini
 * responses into structured, validated, UI-ready data.
 */
export type {
  ChatFormatter,
  CommentaryFormatter,
  EmojiApplier,
  FormatChatParams,
  FormatCommentaryParams,
  FormatterConfig,
  GradeExtractor,
  GradeResult,
  ParsedTextResponse,
  ResponseParser,
} from "./types";
